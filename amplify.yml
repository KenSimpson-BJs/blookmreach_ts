version: 1
frontend:
  phases:
    prebuild:
      commands:
        - yarn install
    build:
      commands:
        - REACT_APP_ENV_P=${REACT_APP_ENV_P}
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
